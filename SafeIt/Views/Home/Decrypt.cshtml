@model SafeIt.Models.FileModel
@{
    ViewData["Title"] = "Decrypt";
    Layout = "~/Views/Shared/_DecryptLayout.cshtml";
}

<div class="col-md-4 mx-auto">
    <form asp-action="Decrypt" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group my-1">
            <label asp-for="Key" class="control-label"></label>
            <input asp-for="Key" type="password" class="form-control" placeholder="Enter key."/>
            <span asp-validation-for="Key" class="text-danger"></span>
        </div>
        <div class="form-group my-1">
            <label asp-for="Document" class="control-label"></label>
            <input asp-for="Document" type="file" class="form-control" />
            <span asp-validation-for="Document" class="text-danger"></span>
        </div>
        @if(!string.IsNullOrEmpty(ViewBag.Download))
        {
            <div class="text-center">
                <a href="@ViewBag.Download" download>Download</a>
            </div>
        }
        <div class="form-group my-2 d-grid">
            <input type="submit" value="Decrypt" class="btn btn-primary btn-block" />
        </div>
    </form>
</div>
